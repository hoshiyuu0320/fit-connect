# Task 2.3 実装計画: 画像からのカロリー推定

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mobile の食事記録シートで画像（最大 3 枚）+ 任意テキストから Claude Vision により食事のカロリー・PFC を推定し、確認シートで人が修正したうえでメッセージとして送信できるようにする。

**Architecture:** Edge Function `estimate-meal-nutrition` を Vision 対応に拡張（画像有無で Sonnet 4.6 / Haiku 4.5 を動的切替）。Mobile は「挿入」タップ時に画像を `message-photos/${userId}/ai/` に upload、URL を Edge Function に渡して推定。`MealTagForm` State に `Map<File, String>` を持ち、確認シートの「戻る」→「挿入」再試行時に再アップロードを防止。確認後はチャット送信時に upload 済み URL を再利用。

**Tech Stack:** Supabase Edge Function (Deno) / Flutter (Riverpod, Supabase SDK, image_picker, cached_network_image) / Claude API

**Spec:** [docs/tasks/2026-05-09-task-2.3-image-meal-estimation-design.md](./2026-05-09-task-2.3-image-meal-estimation-design.md)

---

## File Structure

| 種別 | パス | 役割 |
|---|---|---|
| Modify | `supabase/functions/estimate-meal-nutrition/index.ts` | image_urls サポート、Vision blocks、動的モデル選択、EMPTY_RESULT エラーコード |
| Modify | `fit-connect-mobile/lib/services/storage_service.dart` | `uploadAiImage` / `uploadAiImages` 追加 |
| Modify | `fit-connect-mobile/lib/features/meal_records/data/meal_estimation_api.dart` | `imageUrls` パラメータ + `emptyResult` エラーコード |
| Modify | `fit-connect-mobile/lib/features/messages/presentation/widgets/structured_tag_form.dart` | `Map<File, String>` 状態、画像アップロード→推定フロー、画像のみでも推定可能化 |
| Modify | `fit-connect-mobile/lib/features/messages/presentation/widgets/meal_estimation_confirm_view.dart` | 読み取り専用の画像サムネイル表示 |
| Modify | `fit-connect-mobile/lib/features/messages/presentation/widgets/chat_input.dart` | `onSendWithEstimation` に `preUploadedUrls` 引数追加、再 upload skip |
| Create | `fit-connect-mobile/test/services/storage_service_test.dart` | `uploadAiImage` のパス生成テスト |
| Modify | `docs/tasks/IMPLEMENTATION_TASKS.md` | 2.3 を完了マーク |

---

## Task 1: Edge Function — Vision サポート + 動的モデル選択

**Files:**
- Modify: `supabase/functions/estimate-meal-nutrition/index.ts`

**根拠:** Spec §4.1。既存の `image_urls` 無視を解除し、Sonnet 4.6 への動的切替、コンテンツ空時の許容（画像があれば）、EMPTY_RESULT エラーコード追加、Vision 用 system prompt 追記、タイムアウトを 30 秒に延長。

- [ ] **Step 1.1: SYSTEM_PROMPT を Vision 対応に拡張**

`supabase/functions/estimate-meal-nutrition/index.ts:9-22` の `SYSTEM_PROMPT` 文字列を以下に置き換える:

```typescript
const SYSTEM_PROMPT = `あなたは栄養素推定アシスタントです。日本語の食事内容テキストおよび/または食事画像から、食品ごとに推定値（カロリー・タンパク質・脂質・炭水化物）を計算します。

返却形式は厳密に以下の JSON のみ（説明文・コードフェンス禁止）:
{
  "foods": [{"name": string, "calories": number, "protein_g": number, "fat_g": number, "carbs_g": number}],
  "totals": {"calories": number, "protein_g": number, "fat_g": number, "carbs_g": number}
}

指針:
- 一般的な日本食データベースの値を参考に推定する
- 量が明示されていない場合は「標準的な1人前」として推定する
- 信頼できないものは推定せず foods を空配列で返す
- すべて整数（小数点以下は切り捨て）
- totals は foods の合計と一致させる
- 画像が複数枚ある場合は同一食事の異なる視点／別皿として扱い、合計値を返す
- 画像が食事と無関係（人物、風景、ミーム等）と判断したら foods を空配列で返す
- テキスト補足は画像から得た情報の修正に優先する（例:「ご飯大盛り」なら米飯の量を増やす）`
```

- [ ] **Step 1.2: `callClaude` シグネチャと実装を Vision 対応に変更**

`supabase/functions/estimate-meal-nutrition/index.ts:26-62` の `callClaude` 関数を以下に置き換える:

```typescript
async function callClaude(
  apiKey: string,
  mealType: string,
  content: string,
  imageUrls: string[],
): Promise<any> {
  const controller = new AbortController()
  const timeoutId = setTimeout(() => controller.abort(), 30_000)
  try {
    const hasImages = imageUrls.length > 0
    const model = hasImages ? 'claude-sonnet-4-6' : 'claude-haiku-4-5'

    const userBlocks: any[] = []
    for (const url of imageUrls) {
      userBlocks.push({ type: 'image', source: { type: 'url', url } })
    }
    const textPart = content && content.trim().length > 0
      ? `食事タイプ: ${mealType}\n補足: ${content.trim()}`
      : `食事タイプ: ${mealType}\n補足: (なし、画像のみ)`
    userBlocks.push({ type: 'text', text: textPart })

    const resp = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: {
        'x-api-key': apiKey,
        'anthropic-version': '2023-06-01',
        'content-type': 'application/json',
      },
      signal: controller.signal,
      body: JSON.stringify({
        model,
        max_tokens: 1024,
        temperature: 0.2,
        system: [
          { type: 'text', text: SYSTEM_PROMPT, cache_control: { type: 'ephemeral' } },
        ],
        messages: [{ role: 'user', content: userBlocks }],
      }),
    })
    if (!resp.ok) {
      const text = await resp.text()
      throw new Error(`Claude API ${resp.status}: ${text}`)
    }
    const data = await resp.json()
    const textBlock = data.content?.find((b: any) => b.type === 'text')
    if (!textBlock?.text) throw new Error('No text in Claude response')
    const jsonText = extractJson(textBlock.text)
    return JSON.parse(jsonText)
  } finally {
    clearTimeout(timeoutId)
  }
}
```

- [ ] **Step 1.3: リクエスト body のパースと validation を更新**

`supabase/functions/estimate-meal-nutrition/index.ts:175-186` のセクションを以下に置き換える:

```typescript
    // 4. リクエスト body パース
    const body = await req.json().catch(() => null)
    if (!body) return errorResponse('INVALID_INPUT', 'Invalid JSON body', 400)
    const { meal_type, content } = body
    const rawImageUrls = body.image_urls
    const imageUrls: string[] = Array.isArray(rawImageUrls)
      ? rawImageUrls.filter((u): u is string => typeof u === 'string' && u.length > 0).slice(0, 3)
      : []

    if (!['breakfast', 'lunch', 'dinner', 'snack'].includes(meal_type)) {
      return errorResponse('INVALID_INPUT', 'Invalid meal_type', 400)
    }
    const contentStr = typeof content === 'string' ? content : ''
    if (contentStr.trim().length === 0 && imageUrls.length === 0) {
      return errorResponse('INVALID_INPUT', 'Empty content and no images', 400)
    }
```

- [ ] **Step 1.4: `callClaude` 呼び出し箇所と EMPTY_RESULT エラーコードを更新**

`supabase/functions/estimate-meal-nutrition/index.ts:223-249` のブロックを以下に置き換える:

```typescript
    // 6. Claude 呼び出し
    let result
    try {
      const raw = await callClaude(anthropicKey, meal_type, contentStr, imageUrls)
      result = validateEstimation(raw)
    } catch (e) {
      console.error('Claude call/parse failed:', e)
      await supabase.from('ai_estimation_logs').insert({
        client_id: authUid,
        trainer_id: client.trainer_id,
        function_name: FUNCTION_NAME,
        status: 'error',
        error_code: 'ESTIMATION_FAILED',
      })
      return errorResponse('ESTIMATION_FAILED', 'Estimation failed', 500)
    }

    if (result.foods.length === 0) {
      await supabase.from('ai_estimation_logs').insert({
        client_id: authUid,
        trainer_id: client.trainer_id,
        function_name: FUNCTION_NAME,
        status: 'error',
        error_code: 'EMPTY_RESULT',
      })
      return errorResponse('EMPTY_RESULT', 'No foods could be identified', 422)
    }
```

注: 既存はエラーコードもステータスコードも `ESTIMATION_FAILED` / 500 だったが、Vision 由来の `foods=[]` を Mobile 側で UX 分岐できるよう専用コード `EMPTY_RESULT` / 422 を導入する。

- [ ] **Step 1.5: ローカルでデプロイ前のシンタックス確認**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT
deno check supabase/functions/estimate-meal-nutrition/index.ts
```

Expected: エラーなし（`// @ts-nocheck` のため Deno check は通過する想定）。

- [ ] **Step 1.6: コミット**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT
git add supabase/functions/estimate-meal-nutrition/index.ts
git commit -m "$(cat <<'EOF'
feat(edge): add Vision support to estimate-meal-nutrition

Accept image_urls (max 3), select claude-sonnet-4-6 when images
present, claude-haiku-4-5 otherwise. Allow empty content when
images attached. Return EMPTY_RESULT (422) when foods=[] so the
client can branch UX. Extend timeout to 30s for Vision latency.
EOF
)"
```

- [ ] **Step 1.7: Edge Function を Supabase にデプロイ**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT
supabase functions deploy estimate-meal-nutrition --no-verify-jwt
```

Expected: `Deployed Function estimate-meal-nutrition` 表示。

注: Mobile 側未対応のためテキスト推定（image_urls 無し）の挙動は変わらないことを実機で 1 回確認してから次タスクへ進む。

---

## Task 2: Mobile — `StorageService.uploadAiImage`

**Files:**
- Modify: `fit-connect-mobile/lib/services/storage_service.dart`
- Create: `fit-connect-mobile/test/services/storage_service_test.dart`

**根拠:** Spec §4.2。AI 由来画像を `${userId}/ai/${uuid}.jpg` のパスで分離 upload する。orphan 識別のためのプレフィックスのみで、テーブル追加はしない。

- [ ] **Step 2.1: パス生成のための pure function を追加（テスト容易化）**

`fit-connect-mobile/lib/services/storage_service.dart` の `class StorageService { ... }` 内、既存の `uploadImage` 直前あたりに追加:

```dart
  /// AI 由来の画像を保存するときの相対パス（テスト容易化のため pure に切り出し）
  static String aiImagePath(String userId, String uuid) {
    return '$userId/ai/$uuid.jpg';
  }
```

- [ ] **Step 2.2: `uploadAiImage` / `uploadAiImages` を追加**

`fit-connect-mobile/lib/services/storage_service.dart` の `uploadImages` 関数（並列・逐次関係なくシリアル実行されている既存関数）の直後に追加:

```dart
  /// AI 推定で利用する画像を Supabase Storage にアップロード（プレフィックス分離）
  /// パス形式: `${userId}/ai/${uuid}.jpg`
  /// orphan 識別のため、通常のメッセージ画像 (`${userId}/${uuid}.jpg`) とフォルダで分離する。
  static Future<String?> uploadAiImage(File file, String userId) async {
    try {
      final filePath = aiImagePath(userId, _uuid.v4());
      await SupabaseService.client.storage
          .from(bucketName)
          .upload(filePath, file);
      final publicUrl = SupabaseService.client.storage
          .from(bucketName)
          .getPublicUrl(filePath);
      debugPrint('[StorageService] AI uploaded: $publicUrl');
      return publicUrl;
    } catch (e) {
      debugPrint('[StorageService] uploadAiImage error: $e');
      return null;
    }
  }

  /// 複数の AI 用画像を並列アップロード（推定レイテンシ短縮）
  /// 戻り値は files と同じ順序、失敗した要素は null
  static Future<List<String?>> uploadAiImages(
      List<File> files, String userId) async {
    if (files.isEmpty) return const [];
    return Future.wait(files.map((f) => uploadAiImage(f, userId)));
  }
```

注: 通常の `uploadImages` はシリアルだが、Vision 推定はレイテンシが長いため AI 用は並列化する。

- [ ] **Step 2.3: パス生成のテストを書く（失敗確認）**

`fit-connect-mobile/test/services/storage_service_test.dart` を新規作成:

```dart
import 'package:fit_connect_mobile/services/storage_service.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('StorageService.aiImagePath', () {
    test('returns "${userId}/ai/${uuid}.jpg" format', () {
      final path = StorageService.aiImagePath('user-123', 'abcd-uuid');
      expect(path, 'user-123/ai/abcd-uuid.jpg');
    });

    test('separates ai/ prefix from regular message photo path', () {
      final path = StorageService.aiImagePath('user-123', 'xyz');
      expect(path.contains('/ai/'), isTrue);
      expect(path.startsWith('user-123/'), isTrue);
    });
  });
}
```

- [ ] **Step 2.4: テスト実行（失敗 → 成功）**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT/fit-connect-mobile
flutter test test/services/storage_service_test.dart
```

Expected: 2 テストが成功（Step 2.1 で先に `aiImagePath` を追加済みのため通る想定）。
もし `aiImagePath` を Step 2.1 で先に書かずに進めた場合は、まず失敗を確認してから Step 2.1 に戻る。

- [ ] **Step 2.5: コミット**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT
git add fit-connect-mobile/lib/services/storage_service.dart fit-connect-mobile/test/services/storage_service_test.dart
git commit -m "$(cat <<'EOF'
feat(mobile): add StorageService.uploadAiImage with ai/ prefix

AI-derived images go to ${userId}/ai/${uuid}.jpg so future cleanup
jobs can scope to that subfolder. Uploads are parallel (Future.wait)
since Vision latency dominates.
EOF
)"
```

---

## Task 3: Mobile — `MealEstimationApi.estimate` に `imageUrls` パラメータを追加

**Files:**
- Modify: `fit-connect-mobile/lib/features/meal_records/data/meal_estimation_api.dart`

**根拠:** Spec §4.3。`image_urls` をリクエスト body に流す。新規エラーコード `EMPTY_RESULT` を `MealEstimationErrorCode.emptyResult` にマップし、UI で個別 UX に分岐できるようにする。

- [ ] **Step 3.1: `MealEstimationErrorCode` に `emptyResult` を追加**

`fit-connect-mobile/lib/features/meal_records/data/meal_estimation_api.dart:6` を以下に置き換える:

```dart
enum MealEstimationErrorCode { forbidden, rateLimit, invalidInput, estimationFailed, emptyResult, network }
```

- [ ] **Step 3.2: `estimate` メソッドに `imageUrls` 引数を追加**

`fit-connect-mobile/lib/features/meal_records/data/meal_estimation_api.dart:17-28` を以下に置き換える:

```dart
  static Future<MealEstimationResult> estimate({
    required String mealType, // 'breakfast' | 'lunch' | 'dinner' | 'snack'
    required String content,
    List<String> imageUrls = const [],
  }) async {
    try {
      final response = await SupabaseService.client.functions.invoke(
        'estimate-meal-nutrition',
        body: {
          'meal_type': mealType,
          'content': content,
          if (imageUrls.isNotEmpty) 'image_urls': imageUrls,
        },
      );
```

- [ ] **Step 3.3: エラーマッピングに `EMPTY_RESULT` を追加**

`fit-connect-mobile/lib/features/meal_records/data/meal_estimation_api.dart:47-57` の switch 文に `EMPTY_RESULT` ケースを追加:

```dart
      switch (errCode) {
        case 'FORBIDDEN':
          throw MealEstimationException(MealEstimationErrorCode.forbidden, msg);
        case 'RATE_LIMIT':
          throw MealEstimationException(MealEstimationErrorCode.rateLimit, msg);
        case 'INVALID_INPUT':
          throw MealEstimationException(MealEstimationErrorCode.invalidInput, msg);
        case 'EMPTY_RESULT':
          throw MealEstimationException(MealEstimationErrorCode.emptyResult, msg);
        case 'ESTIMATION_FAILED':
        default:
          throw MealEstimationException(MealEstimationErrorCode.estimationFailed, msg);
      }
```

- [ ] **Step 3.4: `flutter analyze` で型整合性を確認**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT/fit-connect-mobile
flutter analyze lib/features/meal_records/data/meal_estimation_api.dart
```

Expected: `No issues found!`（`MealEstimationErrorCode` を使う他箇所は次タスクで対応するため警告なし）。

注: `_humanError` で switch 網羅性が要求される箇所（`structured_tag_form.dart:441`）は次タスクで一緒に修正するため、ここでは未対応で OK。

- [ ] **Step 3.5: コミット**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT
git add fit-connect-mobile/lib/features/meal_records/data/meal_estimation_api.dart
git commit -m "$(cat <<'EOF'
feat(mobile): pass image_urls and map EMPTY_RESULT in MealEstimationApi

Adds imageUrls parameter (omitted from body when empty for
backward compat). Maps the new EMPTY_RESULT error code so the UI
can show a different snackbar than generic estimation failures.
EOF
)"
```

---

## Task 4: Mobile — `MealTagForm` 画像 upload + 推定フロー

**Files:**
- Modify: `fit-connect-mobile/lib/features/messages/presentation/widgets/structured_tag_form.dart`

**根拠:** Spec §4.4。`Map<File, String>` で File↔URL を保持し、確認シートからの「戻る」→「挿入」再試行で再 upload を防ぐ。画像のみでも推定可能化、`EMPTY_RESULT` 用 snackbar、`onSendWithEstimation` に upload 済み URL を渡す変更。

- [ ] **Step 4.1: 必要 import の追加と現在の auth user id 取得経路の確認**

`structured_tag_form.dart` の冒頭 import に追加:

```dart
import 'package:fit_connect_mobile/services/storage_service.dart';
import 'package:fit_connect_mobile/services/supabase_service.dart';
```

- [ ] **Step 4.2: `MealTagForm` の callback 型を更新（preUploadedUrls を受け渡す）**

`structured_tag_form.dart:293` を以下に置き換える:

```dart
  /// PFC込みで送信。preUploadedUrls は AI 推定時に upload 済みの画像 URL（再 upload 回避用）
  /// null の場合は AI推定なしで `onCompose` のみ実行
  final Future<void> Function(
    String composedText,
    MealEstimationResult estimation,
    List<String> preUploadedUrls,
  )? onSendWithEstimation;
```

- [ ] **Step 4.3: State に `_fileToUrlMap` を追加**

`structured_tag_form.dart:310-318`（`_MealTagFormState` のフィールド宣言部）に追加:

```dart
  /// 「戻る」→「挿入」再試行時の再 upload を防ぐため、File と Storage URL の対応を保持。
  /// 確認シートの「送信」時にこの値を `onSendWithEstimation` の preUploadedUrls として渡す。
  final Map<File, String> _fileToUrlMap = {};
```

- [ ] **Step 4.4: `_handleInsert` を画像 upload + Vision 対応に置き換え**

`structured_tag_form.dart:361-426` の `_handleInsert` メソッドを以下に置き換える:

```dart
  Future<void> _handleInsert() async {
    if (!_isValid) return;

    // テキストも画像もない場合は AI 推定をしない（安全網）
    final hasContent = _contentController.text.trim().isNotEmpty;
    final canTryEstimate = widget.onSendWithEstimation != null
        && (hasContent || widget.hasImages);

    if (!canTryEstimate) {
      widget.onCompose(_composedText);
      return;
    }

    bool aiEnabled = false;
    try {
      aiEnabled = await ref.read(aiFeaturesEnabledProvider.future);
    } catch (_) {
      aiEnabled = false;
    }
    if (!mounted) return;

    if (!aiEnabled) {
      widget.onCompose(_composedText);
      return;
    }

    setState(() {
      _phase = _MealFormPhase.loading;
    });

    try {
      // 1. 未 upload の画像のみ upload する（既存 URL は再利用）
      final imageUrls = await _ensureImagesUploaded();
      if (!mounted) return;

      // 全画像 upload に失敗した場合
      if (widget.selectedImages.isNotEmpty && imageUrls.isEmpty) {
        setState(() => _phase = _MealFormPhase.input);
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(
            content: const Text('画像のアップロードに失敗しました'),
            backgroundColor: AppColors.rose800,
            action: SnackBarAction(
              label: 'AIなしで送信',
              textColor: Colors.white,
              onPressed: () => widget.onCompose(_composedText),
            ),
          ),
        );
        return;
      }

      // 2. AI 推定
      final result = await MealEstimationApi.estimate(
        mealType: _mealTypeToEnum(_selectedMealType),
        content: _contentController.text.trim(),
        imageUrls: imageUrls,
      );
      if (!mounted) return;
      setState(() {
        _estimation = result;
        _editableTotals = result.totals;
        _phase = _MealFormPhase.confirm;
      });
    } on MealEstimationException catch (e) {
      if (!mounted) return;
      final msg = _humanError(e);
      setState(() {
        _phase = _MealFormPhase.input;
      });
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(
          content: Text(msg),
          backgroundColor: AppColors.rose800,
          action: SnackBarAction(
            label: 'AIなしで送信',
            textColor: Colors.white,
            onPressed: () => widget.onCompose(_composedText),
          ),
        ),
      );
    }
  }

  /// 未 upload の選択画像のみを Storage にアップロードし、最終的な URL リストを返す。
  /// 「戻る」→「挿入」再試行時、既に `_fileToUrlMap` に存在する File は再 upload しない。
  /// 部分失敗（一部の File だけ upload 失敗）した場合、成功した URL のみで推定を継続する。
  Future<List<String>> _ensureImagesUploaded() async {
    if (widget.selectedImages.isEmpty) return const [];
    final userId = SupabaseService.client.auth.currentUser?.id;
    if (userId == null) return const [];

    // 削除された File に対応するエントリを map から取り除く（orphan は許容）
    _fileToUrlMap.removeWhere((file, _) => !widget.selectedImages.contains(file));

    // 未 upload の File を抽出
    final pending = widget.selectedImages
        .where((f) => !_fileToUrlMap.containsKey(f))
        .toList();

    if (pending.isNotEmpty) {
      final results = await StorageService.uploadAiImages(pending, userId);
      for (var i = 0; i < pending.length; i++) {
        final url = results[i];
        if (url != null) _fileToUrlMap[pending[i]] = url;
      }
    }

    // selectedImages の順序で URL を返す（upload 失敗した File は除外）
    return widget.selectedImages
        .map((f) => _fileToUrlMap[f])
        .whereType<String>()
        .toList();
  }
```

- [ ] **Step 4.5: `_humanError` に `emptyResult` ケースを追加**

`structured_tag_form.dart:441-454` の `_humanError` を以下に置き換える:

```dart
  String _humanError(MealEstimationException e) {
    switch (e.code) {
      case MealEstimationErrorCode.rateLimit:
        return 'AI推定の上限に達しました。しばらくしてからお試しください';
      case MealEstimationErrorCode.network:
        return '通信エラーが発生しました';
      case MealEstimationErrorCode.forbidden:
        return 'AI機能が利用できません';
      case MealEstimationErrorCode.invalidInput:
        return '入力内容が不正です';
      case MealEstimationErrorCode.emptyResult:
        return '画像から食事を識別できませんでした。テキストで補足して再試行してください';
      case MealEstimationErrorCode.estimationFailed:
        return '推定に失敗しました。内容を変えてお試しください';
    }
  }
```

- [ ] **Step 4.6: `_handleSendWithEstimation` を `preUploadedUrls` 付きに更新**

`structured_tag_form.dart:456-472` の `_handleSendWithEstimation` を以下に置き換える:

```dart
  Future<void> _handleSendWithEstimation() async {
    if (_estimation == null || _editableTotals == null) return;
    if (_isSending) return;
    setState(() => _isSending = true);
    final estimationToSend = MealEstimationResult(
      foods: _estimation!.foods,
      totals: _editableTotals!,
    );
    // selectedImages の順序を保ちつつ upload 済み URL を抽出
    final preUploaded = widget.selectedImages
        .map((f) => _fileToUrlMap[f])
        .whereType<String>()
        .toList();
    try {
      await widget.onSendWithEstimation?.call(
        _composedText,
        estimationToSend,
        preUploaded,
      );
    } finally {
      if (mounted) {
        setState(() => _isSending = false);
      }
    }
  }
```

- [ ] **Step 4.7: 確認シートの `MealEstimationConfirmView` 呼び出しに画像 URL を渡す**

`structured_tag_form.dart` 内で `MealEstimationConfirmView(...)` を呼び出している箇所（`_phase == _MealFormPhase.confirm` のブロック、およそ L605-618）に、新規 prop `imageUrls` を追加する。**既存の prop 名は `totals` / `onTotalsChanged` / `onBack` / `onSend` / `isSending`（`meal_estimation_confirm_view.dart:11-26` 参照）なので、これらは絶対にリネームしない。** 既存の `MealEstimationConfirmView(estimation: _estimation!, totals: _editableTotals!, ...)` 呼び出しに `imageUrls` を 1 行追加するだけ:

```dart
MealEstimationConfirmView(
  estimation: _estimation!,
  totals: _editableTotals!,                         // ← 既存のまま、リネームしない
  imageUrls: widget.selectedImages                  // ← この1行のみ新規追加
      .map((f) => _fileToUrlMap[f])
      .whereType<String>()
      .toList(),
  onTotalsChanged: (t) => setState(() => _editableTotals = t),  // ← 既存のまま
  onBack: () => setState(() => _phase = _MealFormPhase.input),  // ← 既存のまま
  onSend: _handleSendWithEstimation,                            // ← 既存のまま
  isSending: _isSending,                                        // ← 既存のまま
)
```

注: 上記は構造例。具体的な引数値は既存呼び出しを温存し、`imageUrls` の 1 行のみ追加すること。`imageUrls` 引数自体は次タスク（Task 5）で ConfirmView に追加するので、本タスクのコンパイルは Task 5 と同時に通るようにまとめてコミットする（Step 4.9 でコミット保留 → Step 5.6 でまとめてコミット）。

- [ ] **Step 4.8: `flutter analyze` でこのファイルが通ることを確認**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT/fit-connect-mobile
flutter analyze lib/features/messages/presentation/widgets/structured_tag_form.dart
```

Expected: `MealEstimationConfirmView` の `imageUrls` 引数がまだ未定義のためエラー有り。Task 5 で解消する想定（Task 4 + 5 をまとめて 1 コミットでも可）。

- [ ] **Step 4.9: Task 5 と一緒にコミット（先に進む）**

このタスクのコミットは Task 5 末尾でまとめて行う。本ステップではコミット保留。

---

## Task 5: Mobile — `MealEstimationConfirmView` に読み取り専用の画像表示

**Files:**
- Modify: `fit-connect-mobile/lib/features/messages/presentation/widgets/meal_estimation_confirm_view.dart`

**根拠:** Spec §2 末尾。確認シートで画像を読み取り専用で表示し、画像変更は input phase の「戻る」経由のみとすることで URL 整合性を保つ。

- [ ] **Step 5.1: `MealEstimationConfirmView` の現状を読む**

```bash
cat /Users/hoshidayuuya/Documents/FIT-CONNECT/fit-connect-mobile/lib/features/messages/presentation/widgets/meal_estimation_confirm_view.dart
```

ファイル末尾の widget tree（Column/Row 構成）と props 定義部分を把握する。

- [ ] **Step 5.2: `imageUrls` prop を追加**

`MealEstimationConfirmView` の `final` フィールド群に追加:

```dart
  /// AI 推定に使った画像の Storage 公開 URL（confirm phase では読み取り専用）。
  /// 空リストならサムネイル領域を描画しない。
  final List<String> imageUrls;
```

コンストラクタに `this.imageUrls = const []` を追加（位置は他の同種フィールドに合わせる）。

- [ ] **Step 5.3: 読み取り専用サムネイル UI を追加**

build メソッド内、見出し（「AI推定結果を確認」）と食品リストの間あたりに次のセクションを差し込む:

```dart
if (imageUrls.isNotEmpty) ...[
  const SizedBox(height: 8),
  SizedBox(
    height: 64,
    child: ListView.separated(
      scrollDirection: Axis.horizontal,
      itemCount: imageUrls.length,
      separatorBuilder: (_, __) => const SizedBox(width: 8),
      itemBuilder: (context, i) => ClipRRect(
        borderRadius: BorderRadius.circular(6),
        child: CachedNetworkImage(
          imageUrl: imageUrls[i],
          width: 64,
          height: 64,
          fit: BoxFit.cover,
          placeholder: (_, __) => Container(
            color: AppColors.of(context).surfaceVariant,
          ),
          errorWidget: (_, __, ___) => Container(
            color: AppColors.of(context).surfaceVariant,
            child: const Icon(Icons.broken_image_outlined, size: 24),
          ),
        ),
      ),
    ),
  ),
  const SizedBox(height: 8),
],
```

必要 import:

```dart
import 'package:cached_network_image/cached_network_image.dart';
```

注: 削除ボタン（× アイコン）は付けない。画像変更は「戻る」→ input phase で行う。

- [ ] **Step 5.4: `flutter analyze` 全体実行で Task 4 + 5 が整合することを確認**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT/fit-connect-mobile
flutter analyze lib/features/messages/presentation/widgets/structured_tag_form.dart lib/features/messages/presentation/widgets/meal_estimation_confirm_view.dart
```

Expected: `No issues found!`

- [ ] **Step 5.5: 既存の Widget Preview 関数があれば imageUrls 付きで 1 つ追加**

`meal_estimation_confirm_view.dart` の末尾に既存の `@Preview` 関数があれば、それに合わせて 1 つ追加。**既存の prop 名 `totals` / `onTotalsChanged` / `onBack` / `onSend` をそのまま使うこと**:

```dart
@Preview(name: 'MealEstimationConfirmView - WithImages')
Widget previewMealEstimationConfirmViewWithImages() {
  final totals = EstimationTotals(calories: 150, proteinG: 5, fatG: 3, carbsG: 27);
  return MaterialApp(
    theme: AppTheme.lightTheme,
    home: Scaffold(
      backgroundColor: const Color(0xFFF5F5F5),
      body: SafeArea(
        child: Padding(
          padding: const EdgeInsets.all(16),
          child: MealEstimationConfirmView(
            estimation: MealEstimationResult(
              foods: [
                EstimatedFood(name: 'オートミール', calories: 150, proteinG: 5, fatG: 3, carbsG: 27),
              ],
              totals: totals,
            ),
            totals: totals,
            imageUrls: const [
              'https://placehold.jp/64x64.png',
              'https://placehold.jp/64x64.png',
            ],
            onTotalsChanged: (_) {},
            onBack: () {},
            onSend: () {},
          ),
        ),
      ),
    ),
  );
}
```

注: モデル `EstimatedFood` / `EstimationTotals` のコンストラクタ引数名は実物に合わせて修正すること（`meal_estimation_result.dart` 参照）。Preview を追加できない構造なら省略可。

- [ ] **Step 5.6: Task 4 + 5 をまとめてコミット**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT
git add fit-connect-mobile/lib/features/messages/presentation/widgets/structured_tag_form.dart fit-connect-mobile/lib/features/messages/presentation/widgets/meal_estimation_confirm_view.dart
git commit -m "$(cat <<'EOF'
feat(mobile): wire image upload + Vision into MealTagForm

MealTagForm now uploads attached images to message-photos/ai/ at
"insert" press, calls Vision via image_urls, and shows them
read-only in the confirm view. A File->URL map prevents re-upload
when the user goes back to the input phase. Empty-result errors
get a dedicated snackbar message.
EOF
)"
```

---

## Task 6: Mobile — `ChatInput._handleSendWithEstimation` で `preUploadedUrls` を再利用

**Files:**
- Modify: `fit-connect-mobile/lib/features/messages/presentation/widgets/chat_input.dart`

**根拠:** Spec §4.5。既存の `_handleSendWithEstimation`（`chat_input.dart:355-401` 付近）が `_uploadImagesIfAny` を呼んでいるため、AI 推定フェーズで upload 済みの URL があれば再 upload を skip するように既存メソッドを直接拡張する。新規ヘルパーは追加せず、既存の送信パイプライン（`widget.onSend(...)` 呼び出し）と metadata の **フラット形状**を温存する（webhook `parse-message-tags` の契約を破らないため）。

- [ ] **Step 6.1: 既存実装を読んで変更スコープを確定する**

```bash
grep -n "onSendWithEstimation\|_handleSendWithEstimation\|MealTagForm\|_selectedImages\|_uploadImagesIfAny\|widget.onSend(" /Users/hoshidayuuya/Documents/FIT-CONNECT/fit-connect-mobile/lib/features/messages/presentation/widgets/chat_input.dart
```

主な確認点:
- `_handleSendWithEstimation(String composedText, MealEstimationResult estimation)` のシグネチャ（`chat_input.dart:355` 付近）
- 内部で `_uploadImagesIfAny()` を呼んでいる行
- `metadata['meal_estimation']` のフラットな構造（`foods` / `calories` / `protein_g` / `fat_g` / `carbs_g`）
- `widget.onSend(composedText, imageUrls, widget.replyToMessageId, metadata)` で送信している行
- `MealTagForm` を build しているブロックで `onSendWithEstimation: _handleSendWithEstimation` を渡している箇所

- [ ] **Step 6.2: `_handleSendWithEstimation` のシグネチャに `preUploadedUrls` を追加**

`chat_input.dart:355-358` 付近の関数宣言を以下に置き換える:

```dart
  Future<void> _handleSendWithEstimation(
    String composedText,
    MealEstimationResult estimation,
    List<String> preUploadedUrls,
  ) async {
```

- [ ] **Step 6.3: `_uploadImagesIfAny` の呼び出しを `preUploadedUrls` 利用に切り替え**

`_handleSendWithEstimation` 関数内の冒頭、`final imageUrls = await _uploadImagesIfAny();` から `if (imageUrls == null) return;` までを以下に置き換える:

```dart
    // AI 推定フェーズで既に upload 済みなら再 upload しない（二重 upload 防止）。
    // _selectedImages が空なら preUploadedUrls も空のまま、上流の検証で挿入は防がれている。
    final List<String> imageUrls = preUploadedUrls;
```

注: 既存の `_uploadImagesIfAny()` は upload 失敗時に `null` を返して early return していたが、AI 推定経路では upload は MealTagForm 側で完了済みのため、ここでは null チェック不要。upload に完全失敗していた場合は MealTagForm が confirm phase に進ませず input phase に戻すので、この経路には到達しない。

- [ ] **Step 6.4: metadata の `meal_estimation` に `source` キーを追加（フラット形状を温存）**

既存の metadata 構築部分（`chat_input.dart:362-369` 付近）を以下に置き換える:

```dart
    final metadata = <String, dynamic>{
      'meal_estimation': {
        'foods': estimation.foods.map((f) => f.toJson()).toList(),
        'calories': estimation.totals.calories,
        'protein_g': estimation.totals.proteinG,
        'fat_g': estimation.totals.fatG,
        'carbs_g': estimation.totals.carbsG,
        'source': imageUrls.isNotEmpty ? 'vision' : 'text',  // ← この1行のみ追加
      },
    };
```

**重要:** 既存の `parse-message-tags` webhook（`supabase/functions/parse-message-tags/index.ts`）はフラット形状（`metadata.meal_estimation.calories` 等）を期待しているため、`...estimation.toJson()` の spread 等で構造を変えないこと。`source` キーを追加するだけで webhook 互換性は保たれる。

- [ ] **Step 6.5: 既存の `widget.onSend(...)` 呼び出しは変更不要**

`widget.onSend(composedText, imageUrls.isEmpty ? null : imageUrls, widget.replyToMessageId, metadata)` の呼び出しはそのまま温存する（Step 6.3 の `imageUrls` 変数の中身が `preUploadedUrls` になっただけ）。`_selectedImages = []` のクリアも既存ロジックそのままで OK。

- [ ] **Step 6.6: `MealEstimationResult.toJson` 存在の確認（既存 2.2 のために生成済み想定）**

```bash
grep -n "toJson\|@JsonSerializable" /Users/hoshidayuuya/Documents/FIT-CONNECT/fit-connect-mobile/lib/features/meal_records/models/meal_estimation_result.dart
```

Expected: `EstimatedFood.toJson()` が `json_serializable` で生成されている（既存 2.2 で利用中）。無ければ build_runner を流し直す。

- [ ] **Step 6.7: `flutter analyze` でファイル全体に問題が無いことを確認**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT/fit-connect-mobile
flutter analyze lib/
```

Expected: `No issues found!`

- [ ] **Step 6.8: コード生成を流す（json_serializable / riverpod が変更を検出した場合に備えて）**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT/fit-connect-mobile
dart run build_runner build --delete-conflicting-outputs
```

Expected: 既存生成物に変更がない（新規モデル追加無しのため）想定。差分が出たらコミットに含める。

- [ ] **Step 6.9: テスト実行**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT/fit-connect-mobile
flutter test
```

Expected: 既存テスト + 新規 storage_service_test が全て通る。

- [ ] **Step 6.10: コミット**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT
git add fit-connect-mobile/lib/features/messages/presentation/widgets/chat_input.dart
git commit -m "$(cat <<'EOF'
feat(mobile): reuse pre-uploaded URLs in ChatInput meal send

When MealTagForm hands back URLs uploaded for Vision estimation,
ChatInput skips _uploadImagesIfAny and threads those URLs straight
into the message. Adds source: 'vision' | 'text' to the
meal_estimation metadata for future precision feedback.
EOF
)"
```

---

## Task 7: 手動 QA（iOS Simulator）

**Files:** なし（QA のみ）

**根拠:** CLAUDE.md「実装後の自動 QA」。サブエージェントによるコード変更が完了したら必ず `ios-simulator-qa` スキルで動作確認する。

- [ ] **Step 7.1: `ios-simulator-qa` スキルを実行**

主担当（マネージャー）が `ios-simulator-qa` スキルを Skill ツール経由で起動し、以下のシナリオを iOS Simulator 上で確認する:

1. Pro プランクライアントで「食事を記録」シートを開く
2. 画像 1 枚を添付（カメラ/ギャラリーどちらか）→「挿入」
3. ローディング → 確認シート遷移、PFC が編集できることを確認、画像が読み取り専用で表示されること
4. 「戻る」→ テキスト追記 →「挿入」→ 画像が再 upload されない（Storage の object 数が増えない）
5. 「送信」→ チャットに画像 + メタデータ付きでメッセージが届くこと
6. 画像のみ + テキスト無しで挿入 → 画像のみで Vision 推定が走ること
7. 画像 3 枚で挿入 → 全画像が Vision に渡されて合計値が返ること
8. ネットワーク切断状態で挿入 → snackbar +「AIなしで送信」が表示されること
9. Free プラン契約者で挿入 → AI が呼ばれず既存挙動（既存テキスト推定と同様）

注意: 7-2, 7-9 は `aiFeaturesEnabledProvider` の挙動次第なので、テストアカウントを切り替えて確認する。

- [ ] **Step 7.2: Storage バケットに `${userId}/ai/` パスでオブジェクトが生成されていることを確認**

Supabase Studio の Storage タブで `message-photos` バケットを開き、テストユーザーの `ai/` フォルダ配下に `.jpg` が生成されていることを目視確認。

---

## Task 8: ドキュメント更新

**Files:**
- Modify: `docs/tasks/IMPLEMENTATION_TASKS.md`

**根拠:** プロジェクト規約。実装の進捗は随時更新。

- [ ] **Step 8.1: 2.3 のチェックボックスを完了マークに更新**

`docs/tasks/IMPLEMENTATION_TASKS.md` のフェーズ 2 タスク 2.3 の節を以下のように更新:

```markdown
- [x] **2.3 画像からのカロリー推定**（2026-05-09 完了）
  - [x] 食事写真撮影/選択 → Edge Function → Claude Vision → 結果表示
  - [x] 画像のアップロード・前処理（リサイズ・圧縮）  // 既存 image_picker quality 80 で対応
  - [x] 複数料理の認識・個別推定  // 最大3枚を1リクエストにまとめて Sonnet 4.6 へ
  - [x] 推定結果の確認・修正フロー（PFC 編集可、画像はロック）
  - 注: image_urls の有無で claude-sonnet-4-6 / claude-haiku-4-5 を動的切替。EMPTY_RESULT エラーコード新設で UI 分岐
  - 注: orphan 画像の cleanup ジョブは未実装（パスは ${userId}/ai/${uuid}.jpg で分離済み、将来課題）
```

- [ ] **Step 8.2: 実装状況サマリー表のフェーズ 2 進捗を更新**

`docs/tasks/IMPLEMENTATION_TASKS.md` 上部の進捗状況・サマリー表を以下のように更新:

```markdown
**進捗状況**: 全体 ~40%（フェーズ1 完了 / フェーズ2 Stage 2 完了：2.1 + 2.2 + 2.3）
**最終更新**: 2026年5月9日 - フェーズ2 Stage 2（2.3 画像推定）完了
```

サマリー表のフェーズ 2 行を:

```markdown
| 2 | LLM カロリー計算 | Mobile + Supabase | 60% | 🟡 Stage 2（テキスト+画像推定）完了 / 2.4 統合・2.5 スクショ未着手 |
```

- [ ] **Step 8.3: コミット**

```bash
cd /Users/hoshidayuuya/Documents/FIT-CONNECT
git add docs/tasks/IMPLEMENTATION_TASKS.md
git commit -m "$(cat <<'EOF'
docs(tasks): mark phase 2 task 2.3 as complete

Image meal estimation with dynamic Sonnet/Haiku selection is
shipped. Phase 2 progress moves from 40% to ~60%; 2.4 (existing
flow integration) and 2.5 (screenshot parsing) remain.
EOF
)"
```

---

## Verification Checklist（全タスク完了時）

- [ ] `flutter analyze` ですべて clean
- [ ] `flutter test` が全件成功
- [ ] Edge Function deploy 成功、ANTHROPIC_API_KEY が Supabase Secrets に設定済み
- [ ] iOS Simulator で 9 シナリオ全てが期待動作
- [ ] Storage に `${userId}/ai/` 配下のオブジェクトが生成される
- [ ] `messages.metadata.meal_estimation.source` が `'vision'` になることを実機で確認
- [ ] `ai_estimation_logs` に成功/失敗ログが記録されている
- [ ] CLAUDE.md「実装後の自動 QA」に従い `ios-simulator-qa` スキル実行済み

---

## Notes

- **後方互換**: Edge Function は image_urls 無しのリクエストに対し従来通り動作する。Mobile と Edge Function はどちらを先にデプロイしても良い（Edge Function 先行を推奨）。
- **コスト**: 1 食あたり Vision 込みでも数円〜十円規模を想定。Pro プラン + 50/day の合算上限で十分カバー。
- **Orphan**: 確認シートで送信せずに閉じた場合、`${userId}/ai/${uuid}.jpg` が残る。1 食 ~200KB × 推定 orphan 率 5% で月数 GB 規模になり得るため、Storage 使用量メトリクスを来月以降観測する。閾値超えたら別タスクで cleanup ジョブ実装。
- **将来拡張**: タスク 2.5（スクショ解析）はこの基盤を流用予定。Edge Function には `image_type` パラメータを追加するだけで分岐可能（system prompt のみ差し替え）。
